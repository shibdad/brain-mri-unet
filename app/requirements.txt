# CPU-only — Hugging Face Spaces has no GPU on the free tier.
# gradio itself is installed by the Space (from the README sdk), so it's not
# pinned here — that lets HF use a current gradio whose client doesn't hit the
# json-schema bug that crashed the old 4.44 build.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0
opencv-python-headless>=4.11.0
huggingface_hub
